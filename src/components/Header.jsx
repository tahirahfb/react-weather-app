import logo from '../images/weather_app_final_logo.png';

const Header = () => {
    return(
            <header>
                <img src={logo} alt="Weather App" />
            </header>
    )
    }
export default Header;   